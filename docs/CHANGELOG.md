<!-- docgen:auto -->
# Changelog

_Auto-generated by `docgen` from git history (50 commits, newest first; automation commits excluded). Dates are commit dates._

## 2026-08-09
- Rewrite the docs to describe the application that exists (`63d887a`)
- Drop CONTRIBUTING and keep its conventions in the README (`8942451`)
- Add the remaining hook and utility tests (`a7122d4`)
- Add docs, license, CI, and stop tracking build output (`2e5d525`)
- Add a static build of the app for the hosted demo (`5bdbbb1`)
- Rework the dashboard UI and add a frontend test suite (`f96348a`)
- Replace fabricated API metrics with trained models (`c1ff191`)

## 2026-07-04
- docs: auto-update from docs sweep (`81163bb`)

## 2026-06-30
- Add .dispatch-log.md to gitignore (`8966ab7`)
- Add comprehensive project documentation (`471bb0f`)

## 2026-06-25
- Auto-reseed on startup when data is older than 7 days (`2e61df4`)

## 2026-06-23
- Add CLAUDE.md project context (`acffbb7`)

## 2026-05-09
- Upgrade pandas and numpy for Python 3.12 compatibility (`4dfd8f4`)

## 2026-02-13
- Add standalone reseed script for Railway cron job (`fb3ff26`)
- Fix stale seed data and restore date range presets (`c5ae48a`)

## 2026-01-18
- Fix customer acquisition chart and KPI percentages (`0bd6d30`)
- Update backend static assets with new frontend build (`58139d3`)
- Remove Last 7 Days and Year to Date from date range options (`9a8e93e`)

## 2025-12-29
- Fix date label overlap and default to 90-day view (`a544fb3`)
- Update backend static assets with latest frontend build (`76a9df7`)
- Enhance dashboard with consistent data, improved layouts, and blue color scheme (`08caf58`)

## 2025-12-26
- Update README with accurate documentation (`51916c9`)
- Add comprehensive code documentation throughout codebase (`23e2342`)
- Expand cohort retention to 12 months and fix sales team data (`27797e3`)
- Fix UI issues across dashboard pages (`85d8888`)
- Add working export functionality and default to light mode (`cecf614`)

## 2025-12-25
- Rebuild frontend static assets with UI fixes (`cd91f89`)
- Fix UI bugs and add professional README (`ae27e56`)
- Remove ALL zero fallbacks - ensure realistic values everywhere (`28b3e8b`)
- Fix all vs previous period values - ensure NO zeros (`10296f1`)
- Force rebuild - verify KPI change percentages working (`38a8c1c`)
- Remove old vite.svg favicon (`47f6d22`)
- Add full data reactivity for all time period changes (`20b3a6c`)
- Fix data variation for all pages when switching time periods (`6387aec`)
- Add date filtering to all pages and endpoints (`93ccb0b`)
- Fix vs previous period calculations in dashboard API (`2087d63`)
- Fix date filter reactivity, light mode styling, and chart colors (`e0106c4`)
- Fix: donut charts sorted by descending value, always show % labels, fix tooltip text color (`92d71dc`)
- Fix: remove bar chart hover background, fix forecast gap (`baebba9`)
- Add seed endpoint for production database (`6767b02`)
- Fix: handle existing database tables (`10f933f`)
- Trigger redeploy (`b6b56ab`)
- Add Dockerfile for Railway deployment (`c0d630c`)
- Trigger redeploy (`fbc289e`)
- Remove shell scripts - let Railway detect Python (`2f2d3fb`)
- Remove root config files - use backend as root (`10f06c6`)
- Include built frontend for simpler deployment (`1e8bcc1`)
- Fix Railway deployment configuration (`15ae211`)
- Production ready: Railway deployment + bug fixes (`b7b9609`)
- Initial commit: Enterprise Analytics Dashboard (`aae1912`)
